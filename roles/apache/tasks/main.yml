---
- name: Load OS specific variables
  include_vars: "{{ ansible_os_family }}.yml"

- name: Install Apache
  package:
    name: "{{ apache_package }}"
    state: present

- name: Start Apache
  service:
    name: "{{ apache_service }}"
    state: started
    enabled: yes